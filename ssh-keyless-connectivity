root@ip-172-31-83-83:/etc/ansible# history
    1  apt update -y
    2  python3 --version
    3  ansible version
    4  apt install software-properties-common
    5  apt-add-repository --yes --update ppa:ansible/ansible
    6  apt update -y
    7  apt install ansible
    8  ansible version
    9  ansible --version
   10  cd /etc/ansible/
   11  ls -l
   12  clear
   13  ls -l
   14  cat hosts
   15  l -l
   16  ls -l
   17  vi hosts
   18  cat hosts
   19  ansible -m ping host1
   20  ansible -m ping host2
   21  ansible -m ping DEV
   22  cd /
   23  ls -la
   24  pwd
   25  ls -l
   26  cd ~
   27  pwd
   28  ls -la
   29  pwd
   30  cd .ssh
   31  ls -l
   32  cat authorized_keys
   33  ssh-keygen
   34  ls -l
   35  cat id_rsa.pub
   36  cd /etc/ansible/
   37  ls -l
   38  ansible -m ping DEV
   39  ansible -m ping host1
   40  touch first-playbook.yaml
   41  vi first-playbook.yaml
   42  ansible-playbook first-playbook.yaml --syntax-check
   43  ansible-playbook first-playbook.yaml
   44  cat first-playbook.yaml
   45  vi first-playbook.yaml
   46  ls -l
   47  touch test-script.sh
   48  vi test-script.sh
   49  cat first-playbook.yaml
   50  ansible-playbook first-playbook.yaml --syntx-check
   51  ansible-playbook first-playbook.yaml --syntax-check
   52  ansible-playbook first-playbook.yaml
   53  ls -l
   54  cat test-script.sh
   55  ls -l
   56  cat first-playbook.yaml
   57  cd ..
   58  ls -l
   59  cd ansible/
   60  ls -l
   61  vi ansible.cfg
   62  cat first-playbook.yaml
   63  vi first-playbook.yaml
   64  cat first-playbook.yaml
   65  history
